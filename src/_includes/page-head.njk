<div class="page-head">
  <p class="eyebrow">{{ headEyebrow }}</p>
  <p class="booking-back">
    <a href="{{ headBackHref }}">
      <span class="booking-back__arrow" aria-hidden="true">←</span>
      <span class="booking-back__label">{{ headBackLabel }}</span>
    </a>
  </p>
</div>
